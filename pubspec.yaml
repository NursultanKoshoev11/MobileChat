name: mobile_chat
version: 0.1.0+1
publish_to: none

description: Telegram-style cross-platform group chat app without private chats.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

flutter:
  uses-material-design: true

name: Koom Android APK

on:
  workflow_dispatch:
    inputs:
      api_base_url:
        description: "Backend API base URL for the Android build."
        required: false
        default: "https://koom.servemp3.com"
  push:
    branches:
      - main

permissions:
  contents: read

env:
  DEFAULT_API_BASE_URL: "https://koom.servemp3.com"

jobs:
  build-android:
    name: Koom Android CI / APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Show Flutter doctor
        run: flutter doctor -v

      - name: Ensure Android wrapper files exist
        run: flutter create --platforms=android --project-name mobile_chat .

      - name: Restore project Android configuration
        run: |
          git checkout -- \
            android/settings.gradle \
            android/build.gradle \
            android/app/build.gradle \
            android/app/proguard-rules.pro \
            android/app/src/main/AndroidManifest.xml \
            android/app/src/debug/AndroidManifest.xml \
            android/app/src/main/kotlin/com/nursultankoshoev/mobilechat/MainActivity.kt \
            android/app/src/main/res/values/styles.xml \
            android/app/src/main/res/values/colors.xml \
            android/app/src/main/res/drawable/launch_background.xml \
            android/app/src/main/res/drawable/ic_launcher_foreground.xml \
            android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml

      - name: Verify Firebase Android config
        env:
          GOOGLE_SERVICES_JSON_B64: ${{ secrets.GOOGLE_SERVICES_JSON_B64 }}
        run: |
          if [ -s android/app/google-services.json ]; then
            echo "Using committed android/app/google-services.json"
          elif [ -n "$GOOGLE_SERVICES_JSON_B64" ]; then
            echo "$GOOGLE_SERVICES_JSON_B64" | base64 --decode > android/app/google-services.json
            echo "Created android/app/google-services.json from GOOGLE_SERVICES_JSON_B64 secret"
          else
            echo "Firebase config is required for FCM push notifications. Add android/app/google-services.json or set GOOGLE_SERVICES_JSON_B64 secret."
            exit 1
          fi
          test -s android/app/google-services.json

      - name: Create Android local.properties
        run: echo "flutter.sdk=$FLUTTER_ROOT" > android/local.properties

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze Dart code
        run: flutter analyze

      - name: Run Flutter tests
        run: flutter test

      - name: Resolve API base URL
        run: |
          API_BASE_URL="${{ github.event.inputs.api_base_url }}"
          if [ -z "$API_BASE_URL" ]; then
            API_BASE_URL="$DEFAULT_API_BASE_URL"
          fi
          if [ -z "$API_BASE_URL" ]; then
            echo "API_BASE_URL is required"
            exit 1
          fi
          case "$API_BASE_URL" in
            https://*) echo "API_BASE_URL is valid: $API_BASE_URL" ;;
            *) echo "API_BASE_URL must start with https://" && exit 1 ;;
          esac
          echo "API_BASE_URL=$API_BASE_URL" >> "$GITHUB_ENV"

      - name: Build ARM64 profile APK
        run: |
          echo "Building profile APK with API_BASE_URL=$API_BASE_URL"
          flutter build apk --profile --target-platform android-arm64 -t lib/main.dart --dart-define=API_BASE_URL="$API_BASE_URL"

      - name: Verify APK does not contain x86 native libraries
        run: |
          APK_PATH="build/app/outputs/flutter-apk/app-profile.apk"
          unzip -l "$APK_PATH" | tee apk-contents.txt
          if grep -E "lib/(x86|x86_64)/" apk-contents.txt; then
            echo "APK still contains x86/x86_64 native libraries"
            exit 1
          fi

      - name: Prepare APK artifact
        run: |
          mkdir -p build-artifacts
          cp build/app/outputs/flutter-apk/app-profile.apk build-artifacts/Koom-profile-arm64.apk
          sha256sum build-artifacts/Koom-profile-arm64.apk > build-artifacts/Koom-profile-arm64.sha256.txt
          ls -lah build-artifacts

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: Koom-profile-arm64-apk
          path: |
            build-artifacts/Koom-profile-arm64.apk
            build-artifacts/Koom-profile-arm64.sha256.txt
          if-no-files-found: error
          retention-days: 14
